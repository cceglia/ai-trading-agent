PORT=3000
PYTHON_CMD=python
CORS_ORIGINS=http://localhost:5173

# Data directory (defaults to analyzer/data/ relative to the server's build output).
# Set to override; must match TRADING_ANALYSIS_CACHE_DIR in the analyzer's .env.
# TRADING_ANALYSIS_CACHE_DIR=/app/data
